<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}ForgeOps{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>
        <nav class="navbar navbar-dark bg-dark mb-4">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_dashboard') }}">ForgeOps</a>
                <div>
                    <a href="{{ path('app_incident_index') }}" class="btn btn-outline-light btn-sm">Incidents</a>
                    <a href="{{ path('app_incident_new') }}" class="btn btn-primary btn-sm">+ Nouvel Incident</a>
                </div>
            </div>
        </nav>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>